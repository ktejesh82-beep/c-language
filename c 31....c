#include <stdio.h>

int main() {
    int n, i;
    int arr[100];

    printf("enter ");
    scanf("%d", &n);

    printf(" %d ", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    
    

    return 0;
}
