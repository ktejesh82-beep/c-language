#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int i = 0;

    
    srand(time(NULL));

    
    while (i < 5) {
        int randomNumber = rand() % 100; // 0 to 99
        printf("Random Number %d: %d\n", i + 1, randomNumber);
        i++;
    }

    return 0;
}
