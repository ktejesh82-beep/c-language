#include <stdio.h>

int main()
{
    char str[1000];
    int i;

    fgets(str, sizeof(str), stdin);

    for(i = 0; str[i] != '\0'; i++)
    {
        if(str[i] == ' ')
            printf("\n");
        else if(str[i] != '\n')
            printf("%c", str[i]);
    }

    return 0;
}
